The module adds "Confirm E-Mail" input right after regular e-mail field
at user registration form, and verifies that the emails match.

The module does not have any configuration, the input is added when the
module is enabled.
